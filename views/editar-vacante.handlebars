<main class="contenedor">
  <form action="/vacantes/editar/{{vacante.url}}" method="POST" class="default-form">
    <h3>Información General</h3>

    <div class="campo">
      <label for="">Titulo</label>
      <input type="text" name="titulo" placeholder="Ej: React Developer" value="{{vacante.titulo}}" required>
    </div>

    <div class="campo">
      <label for="">Empresa</label>
      <input type="text" name="empresa" placeholder="Empresa Contrata" value="{{vacante.empresa}}" required>
    </div>

    <div class="campo">
      <label for="">Ubicaión</label>
      <input type="text" name="ubicacion" placeholder="Ej: Venezuela o Romoto" value="{{vacante.ubicacion}}" required>
    </div>

    <div class="campo">
      <label for="">Salario (USD)</label>
      <input type="number" min="0" name="salario" placeholder="Ej: $500 USD" value="{{vacante.salario}}" required>
    </div>

    <div class="campo">
      <label>Contrato</label>
      <select name="contrato" value="{{vacante.contrato}}">
        <option value="" disabled>-- Selecciona --</option>
        <option value="freelance">Frelance</option>
        <option value="Tiempo Completo">Tiempo Completo</option>
        <option value="Medio Tiempo">Medio Tiempo</option>
        <option value="Por Proyecto">Por Proyecto</option>
      </select>
    </div>

    <h3>Descripción del Puesto</h3>
    
    <div class="campo descripcion">
      <label>Descripción</label>
      <input type="hidden" id="x" name="descripcion" value="{{vacante.descripcion}}">
      <trix-editor input="x"></trix-editor>
    </div>

    <h3>Conocimientos</h3>
    <ul class="lista-conocimientos">
      {{#seleccionarSkills vacante.skills}}

      {{/seleccionarSkills}}
    </ul>

    <div class="campo centrar-horizontal">
      <input type="hidden" name="skills" id="skills">
      <input type="submit" value="Guardar Cambios" class="btn btn-azul">
    </div>

  </form>
</main>